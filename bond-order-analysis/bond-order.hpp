#include <common/maximum_struct.hpp>
#include <cstdio>

template <typename T> void bond_order_analysis(Maximum<T>* input, int input_len, double h0, double h1, double h2, double _ths, double _boxsize, double _rlim, FILE* fp);
